/ フォルダであることを表すのに使う
" ファイルやIDの区切りに使う


ファイル名とIDの間は固定長 parserは空白の数を考慮しない

"src"/                 "`{}+*"
	"main.cpp"          "`+*{}"
	"include"/          "*`+{}"
		"foo.h"          "{*`+}"
"README.md"            "`*+{}"
