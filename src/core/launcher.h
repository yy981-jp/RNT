#pragma once

#include <array>
#include <string>

// #include <reproc++/run.hpp>

inline void launchTextEditor(const std::string& editorPath, const std::string& path) {
	// const std::array<std::string, 2> command{ editorPath, path };
	// int status = -1;
	// std::error_code ec;

	// reproc::options options{};
	// options.redirect.parent = true;

	// std::tie(status, ec) = reproc::run(command, options);
}
